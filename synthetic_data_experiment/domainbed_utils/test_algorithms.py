from algorithms import ERM
import torch
import numpy as np

# batch_data = torch.randn((128, 16))
# labels = torch.tensor(np.random.choice([-1, 1], size=(128,)))
# batches =zip(batch_data, labels)
# # breakpoint()
# configs = {
#     'lr': 0.01,
#     'weight_decay': 0.01,
# }

# algorithm = ERM(input_shape=16, num_classes=2, num_domains=2, hparams=configs)


if __name__ == '__main__':
    import os
    